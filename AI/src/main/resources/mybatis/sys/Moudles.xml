<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- 不使用namespace的话sql搜索定位会比较方便 -->
<mapper namespace="Moudles">

	<resultMap id="rs_base" type="Moudles">
        <result property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="path" column="path"/>
        <result property="description" column="description"/>
        <result property="state" column="state"/>
	</resultMap>

	<!--查询表信息关联信息-->
	<resultMap id="rs_base_all" type="Moudles" extends="rs_base">
		<collection property="moudlesFilesList" column="{moudleCode=code}" select="MoudlesFiles.query"/>
	</resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="Moudles.columns">
		id,code,name,path,description,state
	</sql>

	<insert id="Moudles.insert" useGeneratedKeys="true" keyProperty="id" parameterType="Moudles">
        INSERT INTO `moudles` (
        	id ,
        	code ,
        	name ,
        	path ,
        	description ,
        	state 
        ) VALUES (
        	#{id} ,
        	#{code} ,
        	#{name} ,
        	#{path} ,
        	#{description} ,
        	#{state} 
        )
		<selectKey keyProperty="id" resultType="long">
			select LAST_INSERT_ID()
		</selectKey>
	</insert>

	<update id="Moudles.update" parameterType="Moudles">
        UPDATE `moudles`
		<trim prefix="set" suffixOverrides=",">
			<if test="name!=null and name!=''">
	        name = #{name} ,
		    </if>
			<if test="path!=null and path!=''">
	        path = #{path} ,
		    </if>
			<if test="description!=null and description!=''">
	        description = #{description} ,
		    </if>
			<if test="state!=null and state!=''">
	        state = #{state} 
		    </if>
		</trim>
        WHERE
	        id = #{id}  AND 
	        code = #{code} 
	</update>

    <delete id="Moudles.delete">
        DELETE FROM `moudles`
		<include refid="Moudles.where"/>
    </delete>

    <select id="Moudles.load" resultMap="rs_base_all">
		SELECT <include refid="Moudles.columns" />
		    FROM `moudles`
		<include refid="Moudles.where"/>
	</select>

	<sql id="Moudles.where">
		<where>
			<if test="id!=null and id!=''">
				AND id = #{id}
			</if>
			<if test="code!=null and code!=''">
				AND code = #{code}
			</if>
			<if test="name!=null and name!=''">
				AND name = #{name}
			</if>
			<if test="path!=null and path!=''">
				AND path = #{path}
			</if>
			<if test="description!=null and description!=''">
				AND description = #{description}
			</if>
			<if test="state!=null and state!=''">
				AND state = #{state}
			</if>
		</where>
	</sql>



    <select id="Moudles.count" resultType="long">
        SELECT count(*) FROM `moudles`
		<include refid="Moudles.where"/>
    </select>


    <select id="Moudles.query" resultMap="rs_base_all">
    	SELECT <include refid="Moudles.columns" />
	    FROM `moudles`
		<include refid="Moudles.where"/>

		<if test="sortColumns!=null and sortColumns!=''">
			ORDER BY ${sortColumns}
		</if>
    </select>


</mapper>

