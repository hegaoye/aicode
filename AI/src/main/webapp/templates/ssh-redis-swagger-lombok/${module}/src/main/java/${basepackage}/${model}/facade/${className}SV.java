/*
 * ${copyright}
 */
package ${basePackage}.${model}.facade;

import java.util.List;

import ${basePackage}.core.base.BaseService;
import ${basePackage}.${model}.entity.${className};

/**
 * ${notes}
 *
 * @author ${author}
 */
public interface ${className}SV extends BaseService<${className}>{

}
