<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- 不使用namespace的话sql搜索定位会比较方便 -->
<mapper namespace="ProjectJob">

	<resultMap id="rs_base" type="ProjectJob">
        <result property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="number" column="number"/>
        <result property="state" column="state"/>
	</resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="ProjectJob.columns">
		id,code,name,description,number,state
	</sql>

	<insert id="ProjectJob.insert" useGeneratedKeys="true" keyProperty="id" parameterType="ProjectJob">
        INSERT INTO `project_job` (
        	id ,
        	code ,
        	name ,
        	description ,
        	number ,
        	state 
        ) VALUES (
        	#{id} ,
        	#{code} ,
        	#{name} ,
        	#{description} ,
        	#{number} ,
        	#{state} 
        )
		<selectKey keyProperty="id" resultType="long">
			select LAST_INSERT_ID()
		</selectKey>
	</insert>

	<update id="ProjectJob.update" parameterType="ProjectJob">
        UPDATE `project_job`
		<trim prefix="set" suffixOverrides=",">
			<if test="name!=null and name!=''">
	        name = #{name} ,
		    </if>
			<if test="description!=null and description!=''">
	        description = #{description} ,
		    </if>
			<if test="number!=null and number!=''">
	        number = #{number} ,
		    </if>
			<if test="state!=null and state!=''">
	        state = #{state} 
		    </if>
		</trim>
        WHERE
	        id = #{id}  AND 
	        code = #{code} 
	</update>

    <delete id="ProjectJob.delete">
        DELETE FROM `project_job`
		<include refid="ProjectJob.where"/>
    </delete>

    <select id="ProjectJob.load" resultMap="rs_base">
		SELECT <include refid="ProjectJob.columns" />
		    FROM `project_job`
		<include refid="ProjectJob.where"/>
	</select>

	<sql id="ProjectJob.where">
		<where>
			<if test="id!=null and id!=''">
				AND id = #{id}
			</if>
			<if test="code!=null and code!=''">
				AND code = #{code}
			</if>
			<if test="name!=null and name!=''">
				AND name = #{name}
			</if>
			<if test="description!=null and description!=''">
				AND description = #{description}
			</if>
			<if test="number!=null and number!=''">
				AND number = #{number}
			</if>
			<if test="state!=null and state!=''">
				AND state = #{state}
			</if>
		</where>
	</sql>



    <select id="ProjectJob.count" resultType="long">
        SELECT count(*) FROM `project_job`
		<include refid="ProjectJob.where"/>
    </select>


    <select id="ProjectJob.query" resultMap="rs_base">
    	SELECT <include refid="ProjectJob.columns" />
	    FROM `project_job`
		<include refid="ProjectJob.where"/>

		<if test="sortColumns!=null and sortColumns!=''">
			ORDER BY ${sortColumns}
		</if>
    </select>


</mapper>

